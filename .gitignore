# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Building for u-root generates directory .bb, we dont want that duplicate
*.bb

# Added by cargo

/target

# Go binaries

/bin

# whatever
*~
